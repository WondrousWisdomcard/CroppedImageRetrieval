from setuptools import setup, find_packages

setup(
    name="CroppedImageRetrieval",
    version="0.1",
    url='https://github.com/WondrousWisdomcard/CroppedImageRetrieval.git',
    long_description=open('README.md').read(),
    packages=find_packages(),
)